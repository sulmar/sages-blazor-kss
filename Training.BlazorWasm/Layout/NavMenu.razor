<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Training.BlazorWasm</a>
        @* Przełącznik menu — woła metodę C#. *@
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

@* Klasa CSS zależy od stanu collapseNavMenu; klik zwija menu. *@
<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            @* Aktywny tylko na stronie głównej. *@
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="counter">
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Counter
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="weather">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Weather
            </NavLink>
        </div>
    </nav>
</div>

@code {
    // true — menu zwinięte.
    private bool collapseNavMenu = true;

    // Klasa collapse albo brak klasy, gdy menu jest otwarte.
    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    // Odwraca stan menu; widok odświeża się sam.
    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
